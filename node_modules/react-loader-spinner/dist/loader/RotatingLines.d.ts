import React from 'react';
declare type Props = {
    width?: string;
    visible?: boolean;
    strokeWidth?: string;
    strokeColor?: string;
    animationDuration?: string;
};
export default function RotatingLines({ strokeColor, strokeWidth, animationDuration, width, visible, }: Props): React.ReactElement | null;
export {};
